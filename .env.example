# ========== Required ==========
# GitHub Personal Access Token (needs `repo` scope for private repos)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Gitea instance URL
GITEA_URL=https://your-gitea.com

# Gitea access token (generate in Gitea: Settings → Applications → Generate Token)
GITEA_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxx

# Gitea user/org to own the mirrored repos
GITEA_OWNER=your_username

# ========== Optional ==========
# Check interval in seconds (default: 600 = 10 minutes)
# SYNC_INTERVAL=600

# Gitea mirror sync interval (default: 10m)
# MIRROR_INTERVAL=10m

# Include private repos (default: true)
# INCLUDE_PRIVATE=true

# Include forked repos (default: false)
# INCLUDE_FORKS=false

# Repos to skip, comma-separated (default: empty)
# SKIP_REPOS=repo1,repo2,old-project

# Preview mode - don't actually create mirrors (default: false)
# DRY_RUN=false
